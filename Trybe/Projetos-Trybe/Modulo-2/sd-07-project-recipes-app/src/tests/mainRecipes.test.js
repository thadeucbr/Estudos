// import React from 'react';
// import renderWithRouter from '../components/renderWithRouter';
// import MainRecipes from '../pages/MainRecipes';

describe('25 - Implemente os elementos da tela principal de receitas '
+ 'respeitando os atributos descritos no protótipo', () => {
  test('A tela tem os data-testids de todos os 12 cards da tela de comidas', () => {

  });
  test('A tela tem os data-testids de todos os 12 cards da tela de bebidas', () => {

  });
});

describe('26 - Carregue as 12 primeiras receitas de comidas ou bebidas, '
+ 'uma em cada card', () => {
  test('Caso as receitas sejam de comida, deve-se carregar as 12 '
  + 'primeiras receitas', () => {

  });
  test('Caso as receitas sejam de bebida, deve-se carregar as'
  + ' 12 primeiras receitas', () => {

  });
});

describe('27 - Implemente os botões de categoria para '
+ 'serem utilizados como filtro', () => {
  test('Caso as receitas sejam de comida, deve-se exibir as 5 primeiras categorias'
  + ' de comida', () => {

  });
  test('Caso as receitas sejam de bebida, deve-se exibir as 5 primeiras '
  + 'categorias de bebida', () => {

  });
});

describe('28 - Implemente o filtro das receitas através da API ao clicar no '
+ 'filtro de categoria', () => {
  test('Caso as receitas sejam de comida e a categoria seja "Beef", '
  + 'deve-se carregar as 12 primeiras receitas de "Beef"', () => {

  });
  test('Caso as receitas sejam de comida e a categoria seja "Breakfast", deve-se '
  + 'carregar as 12 primeiras receitas de "Breakfast"', () => {

  });
  test('Caso as receitas sejam de comida e a categoria seja "Chicken", deve-se carregar'
  + ' as 12 primeiras receitas de "Chicken"', () => {

  });
  test('Caso as receitas sejam de comida e a categoria seja "Dessert",'
  + ' deve-se carregar as 12 primeiras receitas de "Dessert"', () => {

  });
  test('Caso as receitas sejam de comida e a categoria seja "Goat"'
  + ', deve-se carregar as 12 primeiras receitas de "Goat"', () => {

  });
  test('Caso as receitas sejam de bebida e a categoria seja '
  + '"Ordinary Drink", deve-se carregar as 12 primeiras receitas de "Ordinary '
  + 'Drink"', () => {

  });
  test('Caso as receitas sejam de bebida e a categoria seja "Cocktail", '
  + 'deve-se carregar as '
  + '12 primeiras receitas de "Cocktail"', () => {

  });
  test('Caso as receitas sejam de bebida e a categoria seja "Milk / Float '
  + '/ Shake", deve-se carregar as 12 primeiras receitas de "Milk / Float / '
  + 'Shake"', () => {

  });
  test('Caso as receitas sejam de bebida e a categoria seja "Other/Unknown",'
  + ' deve-se carregar as 12 primeiras receitas de "Other/Unknown"', () => {

  });
  test('Caso as receitas sejam de bebida e a categoria seja "Cocoa", deve-'
  + 'se carregar as 12 primeiras receitas de "Cocoa"', () => {

  });
});

describe('29 - Implemente o filtro como um toggle, que se for selecionado de '
+ 'novo, o app deve retornar as receitas sem nenhum filtro', () => {
  test('Caso as receitas sejam de comida e o filtro tenha sido selecionado '
  + 'novamente, deve-se retornar as 12 primeiras receitas sem filtro', () => {

  });
  test('Caso as receitas sejam de bebida e o filtro tenha sido '
  + 'selecionado novamente, deve-se retornar as 12 primeiras receitas sem filtro', () => {

  });
});

describe('30 - Implemente o filtro de categoria para que apenas '
+ 'um seja selecionado por vez', () => {
  test('Caso as receitas sejam de comida apenas um filtro de categoria deve poder ser '
  + 'selecionado por vez', () => {

  });
  test('Caso as receitas sejam de bebida apenas um filtro de categoria deve '
  + 'poder ser selecionado por vez', () => {

  });
});

describe('31 - Desenvolva o filtro de categorias com a opção de filtrar'
+ ' por todas as categorias', () => {
  test('Caso as receitas sejam de comida deve existir a opção de filtrar por todas as '
  + 'categorias', () => {

  });
  test('Caso as receitas sejam de bebida deve existir '
  + 'a opção de filtrar por todas as categorias', () => {

  });
});

describe('32 - Redirecione a pessoa usuária, ao clicar no card, para '
+ 'a tela de detalhes, que deve mudar a rota e conter o id da receita na URL', () => {
  test('Caso as receitas sejam de comida a rota deve mudar para a tela de'
  + ' detalhes da receita', () => {

  });
  test('Caso as receitas sejam de bebida a rota deve mu'
  + 'dar para a tela de detalhes da receita', () => {

  });
});
